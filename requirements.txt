# Django core
Django>=4.2,<5.0

# Django REST Framework
djangorestframework>=3.14,<4.0

# Database
psycopg2-binary>=2.9,<3.0  # PostgreSQL adapter

# Celery for async tasks
celery>=5.3,<6.0
redis>=5.0,<6.0  # Broker for Celery

# Django Channels for real-time collaboration
channels>=4.0,<5.0
channels-redis>=4.0,<5.0

# CORS for frontend integration
django-cors-headers>=4.0,<5.0

# Authentication & Security
djangorestframework-simplejwt>=6.0,<7.0  # JWT auth

# AI/ML libraries
numpy>=1.24,<2.0
pandas>=2.0,<3.0
scikit-learn>=1.2,<2.0

# Optional NLP / embeddings (for tutor agent or recommendation engine)
sentence-transformers>=2.2,<3.0
torch>=2.1,<3.0

# Development tools (optional but recommended)
python-dotenv>=1.0,<2.0  # load environment variables
gunicorn>=21.2,<22.0    # production WSGI server

# Optional for testing
pytest>=8.0,<9.0
pytest-django>=4.5,<5.0
